b 100 
command 1
    printf "input[%d] = %s\n", i, input[i]
    set $info = findName(input[i], table)
    if(!strcmp($info->lastName,input[i]))
        printf "find->lastName = %s\n",$info->lastName
    else
        printf "-><- No found. -><- \n"
    end
    c
end
r
